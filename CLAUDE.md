# saaschurch — frontend

Next.js (App Router) + MUI + Redux Toolkit Query. O backend fica em `../saaschurch-api`.

As regras de arquitetura e de UI valem para qualquer alteração neste repo:

@.cursorrules

## Comandos

```bash
pnpm dev                        # sobe o dev server na porta 3000
npx tsc --noEmit -p tsconfig.json   # checagem de tipos
npx eslint <caminho>            # lint
```
